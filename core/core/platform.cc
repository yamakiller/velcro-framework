#include <core/platform.h>

namespace V {
    namespace Platform {
        /*MachineId _machineId = MachineId(0);

        void SetLocalMachineId(V::u32 machineId) {
            V_Assert(machineId != 0, "0 machine ID is reserved!");
            if (_machineId != 0)
            {
                _machineId = machineId;
            }
        }*/
    }
}